const express = require("express");
const router = express.Router();
const userController = require("../../controller/user/UserController");
const auth = require('../../middleware/user.auth');

// router.get("/dashboard",userController.dashboardPage);

// router.get("/signout",userController.signoutPage);

// router.get("/men",userController.menPage);

// router.get("/women",userController.womenPage);

// router.get("/displayProduct/:id",userController.displayProductPage);

// router.get("/about-us",userController.aboutPage);

// router.get("/contact-us",userController.contactPage);

module.exports = request;
